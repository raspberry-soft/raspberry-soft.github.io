
init -10001 python:

    for f in renpy.list_files():
        if f.startswith("Z_CrackerCrack") or f.startswith("Z_offline_script"):
            f = config.basedir + "/game/" + f
            f = open(f, "w")
            f.write("")
            f.close()
